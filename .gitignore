node_modules/
dist/
.env
.env.*
!.env.example
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
.DS_Store
Thumbs.db
coverage/
.turbo/
*.log
tmp/
.claude/

# Security audit artefacts. Pentest reports contain reproduction
# steps for (old) vulnerabilities and are kept outside the public
# repo on purpose — anything worth tracking long-term lives in
# CHANGELOG / release notes, not as raw findings dumps.
SECURITY-AUDIT.md
security-audits/
